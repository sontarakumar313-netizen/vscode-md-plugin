# AGENTS.md

## 开发规范

- 修改前先阅读相关代码、配置与测试。
- 修复 Bug 前必须先复现问题，记录复现步骤、预期结果和实际结果，再定位根因。
- 只做与任务相关的最小改动，禁止顺手重构。
- TypeScript 保持严格类型；不得使用 `any`、`@ts-ignore` 或非空断言掩盖问题。
- 所有异步调用必须正确等待并处理异常；VS Code 的监听器和命令必须正确释放。
- Webview 消息、配置和文件内容等外部输入必须校验。
- 修复 Bug 时应补充回归测试；修改后运行：
  - `npm run typecheck`
  - `npm test`
  - `npm run build`
- 未通过验证时，不得声称任务已经完成，并需说明未验证项及风险。

## 修复记录

每次修复 Bug 或优化代码后，必须在 `00-docs` 下新增 Markdown 文档，说明问题复现、根因、解决方案和验证结果，文件名格式为：`代码修复-时间后缀.md`。
