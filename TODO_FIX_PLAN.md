# TODO 功能修复方案（待审核）

> 状态：**仅完成代码阅读与方案设计，尚未修改任何业务源码。** 你确认本方案后，我才会先复现问题，再按方案实施。

## 一、目标与交互口径

本次只处理本节列出的 6 项，不顺手重构其他模块。代码块顶部窄区域误触发源码框、代码块复制提示两项延期到下次工作。

### 1. 两种模式下的剪切、复制、粘贴

建议统一为以下行为：

| 场景 | 复制 / 剪切到剪贴板 | 粘贴行为 |
| --- | --- | --- |
| 可视化编辑（WYSIWYG）普通内容 | `text/plain` 写入对应 Markdown；同时提供已清理的 `text/html`，方便粘贴到富文本软件时保留样式 | 有安全 HTML 时先清理再转换为 Markdown；否则按纯文本/Markdown 粘贴 |
| 源码分屏（SV）源码区 | 精确保留选中的 Markdown 源码、空格和换行，只写纯文本 | 优先插入 `text/plain`，不引入外部 HTML 样式 |
| 两种模式的代码区域 | 始终按原始纯文本处理，不解析 Markdown/HTML | 保留代码缩进和换行，不产生富文本节点 |
| 图片/音频/文件 | 沿用现有上传流程 | 继续执行大小、MIME、文件签名和路径校验 |

具体交互：

- 同时支持 Windows/Linux 的 `Ctrl+C/X/V`、macOS 的 `Cmd+C/X/V`，以及 Webview 可用的系统剪贴板菜单。
- 没有选区时，复制和剪切不修改文档。
- 剪切必须先成功写入剪贴板，再删除选区；删除后立即同步文档，并且只产生一个可撤销步骤。
- 不接管链接、图片、折叠标题等弹窗里的 `<input>`，这些输入框继续使用浏览器原生剪贴板行为。
- 粘贴内容属于外部输入：检查 `clipboardData`、MIME、文件数量和内容类型，异步上传必须等待并处理失败。
- “More” 菜单里的“复制整篇 Markdown / HTML”保持原功能，本项主要优化编辑区选区的剪贴板行为。

### 2. 左侧标题等级改为下拉菜单

- 仅在 WYSIWYG 模式中，把标题左侧现有的 `H1`～`H6` 标记改为真正可点击、可聚焦的按钮。
- 点击等级标记后，在其附近打开 `H1`～`H6` 菜单；当前等级显示勾选状态。
- 选择新等级后：
  - 标题文本、行内格式和标题位置不变；
  - 光标恢复到原标题；
  - 立即更新 Markdown、目录和撤销栈；
  - 选择当前等级时不产生无效编辑。
- 支持键盘操作：`Enter`/`Space` 打开，方向键切换，`Enter` 确认，`Escape` 关闭；点击外部、滚动或切换模式时关闭。
- 菜单控件使用 `contenteditable="false"` 和 Vditor 的预览标记，确保按钮文字不会写入 Markdown，也不会进入复制内容。
- 标题位于 `<details>` 内容中时也采用相同行为。
- 本次菜单只提供 `H1`～`H6`，不额外加入“正文/段落”。正文转换仍可使用顶部标题工具。

### 3. 顶部工具栏缺少悬浮提示

- 为所有顶部一级按钮检查并补齐非空 `aria-label`，优先使用 Vditor 官方语言包和项目现有多语言文本，不硬编码中文。
- 工具栏固定在窗口顶部时，提示统一显示在按钮下方：左侧按钮向右对齐、中央按钮居中、右侧按钮向左对齐，防止提示跑出视口。
- 覆盖 Vditor 在窄窗口下直接禁用 tooltip 内容的规则，鼠标悬浮和键盘聚焦都应显示提示。
- 下拉菜单中的文字项不重复增加 tooltip；禁用按钮仍保留名称说明；含有下拉菜单的按钮点击后会关闭提示。

### 4. 行内公式和行间公式背景透明

- 对 WYSIWYG、源码分屏预览中的 `.language-math`、KaTeX 容器及对应预览包装层设置透明背景，并移除背景图片。
- 使用 `transparent`，让公式自然继承当前文档背景，而不是写死浅色或深色。
- 仅处理渲染后的公式背景；用户主动打开的公式源码编辑区仍保留可辨识的编辑样式。
- 同步调整内置浅色/深色主题和 `00-styles` 中三套示例主题，避免示例主题重新给公式填色。
- 不改变公式字号、对齐、边距和公式错误提示。

### 5. VSIX 内 README 链接

- 将 `package.json` 的 `homepage`、`bugs.url`、`repository.url` 从不存在/旧的 `vscode-md-interactor` 仓库统一为实际远端仓库：
  `https://github.com/sontarakumar313-netizen/vscode-md-plugin`。
- README 中需要在 VSIX 扩展详情页打开的资源改为稳定的绝对 GitHub URL，包括：logo、`00-styles`、样式说明、CHANGELOG、LICENSE、Releases 和 Issues。
- `00-styles` 继续不打进 VSIX，README 链接直接打开 GitHub 中的目录和文件，避免指向包内不存在的路径。
- 打包后解压检查 VSIX 中的 README，并逐项核对链接目标。

### 6. 顶部功能区快捷键设置及 VS Code 穿透防护

新增统一设置项 `markdown-interactor.toolbarShortcuts`，采用“操作 ID → 快捷键”的对象格式，例如：

```json
{
  "markdown-interactor.toolbarShortcuts": {
    "save": "Mod+S",
    "bold": "Mod+B",
    "math-inline": "Mod+Alt+M",
    "vmd-mode-sv": "Mod+Alt+9"
  }
}
```

快捷键配置规则：

- `Mod` 在 Windows/Linux 映射为 `Ctrl`，在 macOS 映射为 `Cmd`；同时识别 `Ctrl`、`Cmd`/`Meta`、`Alt`/`Option`、`Shift`、字母、数字、方向键和功能键。
- 第一版只支持单段组合键，不支持 `Ctrl+K Ctrl+X` 一类多段 chord。
- 覆盖顶部功能区中的全部可操作按钮，包括一级按钮、标题 H1～H6、编辑模式子项和 More 菜单子项；分隔符不属于操作。
- 空字符串表示禁用该操作的快捷键；删除用户配置则恢复该操作的默认设置。
- 常用编辑操作提供默认快捷键；低频操作允许默认留空，但所有操作均可由用户配置。
- 配置修改后对已打开的 Markdown Interactor 实时生效，无需重新打开文档或重载窗口。
- 按钮 tooltip 和 `aria-label` 显示当前平台实际使用的快捷键；禁用快捷键后不残留旧按键提示。

事件隔离规则：

- 在 Webview 的捕获阶段统一匹配快捷键，先于 Vditor 自带监听器处理。
- 命中有效快捷键后调用 `preventDefault()`、`stopPropagation()` 和 `stopImmediatePropagation()`，并且只触发一次与鼠标点击相同的按钮动作，防止同时触发 VS Code 的侧边栏、命令或面板快捷键。
- 按钮处于禁用状态时不执行动作，但仍消费其已配置快捷键，避免事件穿透到 VS Code。
- 焦点位于链接、图片、折叠标题等弹窗输入框时，不执行格式化动作；输入框保留原生编辑行为，同时阻止已识别的工具快捷键继续冒泡。
- 输入法组合期间不执行工具动作。
- 剪切、复制、粘贴、全选、撤销、重做和查找等基础编辑组合键设为保留键，不允许工具栏配置覆盖。
- 快捷键只在 Markdown Interactor Webview 获得焦点时生效，不注册为全局 VS Code 快捷键。

配置校验与冲突处理：

- Extension Host 校验设置对象、操作 ID、组合键语法和长度；Webview 对接收到的数据再次做类型窄化。
- 未知操作、无修饰键的普通字符、保留组合键和格式错误的值不生效，并通过 VS Code 警告指出具体配置项。
- 两个操作使用同一组合键时，两个冲突项均不执行并提示用户修正，不采用隐式优先级。
- 快捷键控制器只负责调用现有工具栏动作，不复制保存、格式化、上传或模式切换实现。

## 二、初步代码定位

以下是静态阅读得到的定位，正式修改前仍会按“复现 → 记录 → 根因确认”的顺序验证：

| TODO | 初步位置/现象 | 计划涉及文件 |
| --- | --- | --- |
| 剪切/复制/粘贴 | Vditor 3.11.2 的剪切会先 `preventDefault()`，再调用已废弃的 `document.execCommand('delete')`，没有检查结果和失败回退 | 新增 `media-src/src/editor-clipboard.ts`；接入 `media-src/src/main.ts`，必要时补充 `vditor-adapter.ts` |
| 标题等级菜单 | 当前 H1～H6 只是 `::before` 伪元素，天然无法点击或聚焦 | 新增 `media-src/src/wysiwyg-heading-level.ts`；修改 `main.ts`、`main.css`、`lang.ts`，必要时补充 adapter |
| 顶部提示 | 大部分按钮提示方向为 `n`（向上），而工具栏固定在视口顶部；Vditor 还会在窄窗口禁用 tooltip | `media-src/src/toolbar.ts`、`main.css` |
| 公式背景 | 主题/示例主题可对 `.language-math` 设置背景，缺少统一透明规则 | `main.css`、`themes/light.css`、`themes/dark.css`、`00-styles/*.css` |
| README 链接 | Git 远端是 `vscode-md-plugin`，但 `package.json` 元数据仍指向 `vscode-md-interactor`；README 还有指向 VSIX 未包含目录的相对链接 | `package.json`、`README.md` |
| 工具栏快捷键 | 当前快捷键由 Vditor 内部零散处理，自定义按钮没有统一配置入口，部分组合还会继续触发 VS Code | 新增 `media-src/src/toolbar-shortcuts.ts`；修改 `toolbar.ts`、`main.ts`、`lang.ts`、`src/webview.ts`、`src/custom-editor-provider.ts`、`package.json` |

## 三、实施顺序

### 阶段 0：复现并记录（批准后、改代码前）

1. 安装锁定依赖并构建当前基线。
2. 使用 Extension Development Host 准备一个包含普通文本、行内格式、列表、代码块、`$...$`、`$$...$$` 和 H1～H6 的 Markdown 文件。
3. 对每项记录：操作步骤、预期结果、实际结果、当前 Markdown 内容和模式。
4. 特别覆盖 Windows Webview 下的键盘剪切、标题等级菜单、顶部提示、快捷键穿透和浅色/深色主题。
5. 建立当前工具栏快捷键清单，记录每个组合在 WYSIWYG/SV 中是否执行按钮动作、是否重复执行、是否同时触发 VS Code。
6. 只有实际结果与本节目标一致后才进入修改；若某项无法复现，先反馈，不盲改。

### 阶段 1：剪贴板可靠性

1. 增加生命周期可重绑/可释放的剪贴板控制器。
2. 优先使用同步的 `ClipboardEvent.clipboardData` 完成复制/剪切；只在按钮类复制操作中使用异步 Clipboard API 回退。
3. 为 Vditor 的剪切删除失败增加选区删除回退，并触发一次正确的 input/同步/undo。
4. 分别处理 WYSIWYG、SV、代码区和弹窗输入框。

### 阶段 2：标题等级交互

1. 实现标题等级按钮、菜单、定位、键盘操作、关闭和重绑逻辑。
2. 验证等级切换后的 Markdown、目录、光标和撤销栈。

### 阶段 3：工具栏快捷键

1. 定义稳定的工具栏操作 ID、默认快捷键和不可覆盖的保留快捷键集合。
2. 在 Extension Host 校验设置，并监听配置变化后把有效配置发送给当前 Webview。
3. 在 Webview 建立单一快捷键控制器，统一完成匹配、动作分发、事件消费、冲突处理和 tooltip 更新。
4. 禁用会与统一控制器重复执行的 Vditor 私有快捷键入口，同时保留编辑器基础快捷键。
5. 确保配置监听器和 Webview 控制器随面板释放，不产生重复监听。

### 阶段 4：样式和文档

1. 统一顶部 tooltip 方向、边界和窄窗口表现。
2. 加入公式透明背景规则并调整示例主题。
3. 修正包元数据和 README 绝对链接。

### 阶段 5：回归验证

1. 自动化测试全部通过后，再进行两种模式的手工交互检查。
2. 打包 VSIX，检查 README 和包内资源。
3. 最后运行规定的三个命令并如实报告结果。

## 四、回归测试计划

### 自动化交互测试

计划补充以下用例：

- WYSIWYG：复制格式文本同时产生正确 Markdown/plain 与已清理 HTML。
- WYSIWYG：剪切选区后剪贴板正确、DOM 和 Markdown 同步、撤销一次可恢复。
- SV：复制/剪切/粘贴精确保留源码空格和换行。
- 代码区：粘贴 `<b>x</b>` 得到字面文本而不是富文本节点；多行缩进不丢失。
- 弹窗输入框不被编辑区剪贴板控制器拦截。
- H1～H6 标记可打开菜单；改级后 Markdown、目录、光标和 undo 正确；控件文本不泄漏到 Markdown。
- 所有顶部一级按钮具有标签，tooltip 在视口内可见；打开下拉菜单后对应 tooltip 关闭。
- 所有工具栏操作均能读取合法快捷键配置；未知、格式错误、保留和冲突组合不执行。
- 快捷键只执行一次对应按钮动作，且不继续触发 VS Code；分别覆盖 WYSIWYG、SV、禁用按钮和下拉菜单项。
- 修改设置后已打开 Webview 实时更新，tooltip 同步变化；输入框、输入法及基础编辑快捷键不被破坏。
- 行内/行间公式在两种模式、浅色/深色主题下计算背景为透明。
- 包元数据与 README 链接均指向实际仓库。

### 当前测试基础设施问题

当前 `package.json` 的 `npm test` 仍引用多个 `scripts/test-*.js`，但这些测试文件已在提交 `8656f30` 中被删除，因此仓库现状下 `npm test` 无法完整执行。这与本次功能无关，但会阻止满足项目规定的验证命令。

建议在实施时从删除前的版本**原样恢复既有测试脚本和 fixtures**，再只对 Webview 交互测试增加本次回归用例；不修改测试断言来迁就实现。该恢复会增加较多测试文件，但不改变生产代码。

### 最终命令

```bash
npm run typecheck
npm test
npm run build
```

附加打包检查：

```bash
npx @vscode/vsce package --no-dependencies
```

任何命令未通过时，不声明完成，并单独列出未验证项和风险。

## 五、最小改动与风险控制

- 不升级 Vditor，不放宽 Webview CSP，不引入新的运行时依赖。
- 不使用 `any`、`@ts-ignore` 或非空断言掩盖类型问题；新增外部输入均做类型判断。
- 新控制器避免重复注册；Vditor 重建时执行 `rebind`，销毁时释放监听器、Observer 和计时器。
- 工具栏快捷键只通过统一动作入口触发，避免与 Vditor 原有 hotkey 重复执行；配置监听器随编辑器面板正确释放。
- 标题菜单放在可控 UI 层，防止其内容进入 Markdown 序列化、复制结果或撤销快照。
- 保留现有代码块交互、媒体上传、文档同步和模式切换逻辑；本次不修改延期的两项代码块功能。

## 六、请审核时重点确认

1. 是否同意 WYSIWYG 复制同时提供“Markdown 纯文本 + 清理后的 HTML”，而 SV 只复制精确源码？  同意
2. 标题左侧菜单是否确定只需要 H1～H6，不需要“转为正文”？  同意
3. 公式透明背景是否只针对渲染结果，源码编辑框仍保留编辑区背景？  同意
4. 是否同意恢复被删除的既有测试脚本，以便 `npm test` 能按项目规范运行？  同意
5. 快捷键是否采用“所有操作均可配置、常用操作有默认值、低频操作默认留空”？本方案推荐该方式，以减少与 VS Code 和操作系统默认快捷键的冲突。 同意

确认以上口径后，我将严格按本方案先复现、记录，再开始修改。
