### 2. 普通代码块增强

主要文件：wysiwyg-code-block.ts

包含：

- 普通代码块语言栏、编辑、复制按钮。
- 当前新增的同位置、同尺寸原位编辑框。
- 输入字符、Enter、Tab、Backspace、Delete 自动进入编辑。
- 将渲染代码中的光标位置映射到源码位置。
- 超高编辑框随编辑区滚动并保持等高。
- 全块选择、复制、剪切、Delete 删除。
- 代码块前后光标放置和插入段落。
- 隐藏 Vditor 自己的序列化源码。
- Mermaid、Math 等富渲染代码块维持原有弹窗方式。

这个文件还承担了 Math、HTML 等“原子块”的部分选择和删除保护，因此职责偏多。

────────────────────────────────────────────────────────────────────────────────

### 3. 公式和 HTML 源码编辑

主要文件：

- wysiwyg-source-editor.ts
- wysiwyg-html-presentation.ts

功能：

- 块公式、行内公式点击后编辑源码。
- HTML 块、行内 HTML、HTML Entity 编辑。
- 输入时验证并刷新预览。
- HTML 中真实链接、按钮、图片等交互元素不会被误当作编辑触发。
- 支持 `<p align="center">` 等标题对齐展示。
- 隐藏 Vditor 内部源码节点。

────────────────────────────────────────────────────────────────────────────────

### 4. 统一源码弹窗

主要文件：wysiwyg-popover.ts

功能：

- 代码、公式、HTML、Front Matter、Details 标题共用编辑会话。
- 输入字段、错误提示、关闭按钮和 Tab 顺序。
- Escape、点击外部、关闭按钮、Ctrl/Cmd+Enter 结束编辑。
- 控制一次会话只提交一次撤销记录。
- 普通代码原位覆盖、公式侧边/上下方定位、HTML 上下定位。
- 链接和图片 URL 编辑、复制。
- 监听滚动、ResizeObserver 和窗口缩放，持续调整位置。

这是另一个职责明显过重的文件。

────────────────────────────────────────────────────────────────────────────────

### 5. Front Matter

主要文件：

- front-matter.ts
- front-matter-presentation.ts
- wysiwyg-front-matter.ts
- split-preview-presentation.ts

功能：

- 解析 YAML Front Matter。
- 可视化模式显示为表格、代码或隐藏。
- 点击预览打开源码编辑。
- Split View 预览中提供相同展示。
- 修复 Vditor 往返转换时丢失 Front Matter 后空行的问题。

────────────────────────────────────────────────────────────────────────────────

### 6. GitHub Alerts 和引用

主要文件：

- quote-format.ts
- quote-caret.ts
- alert-presentation.ts
- wysiwyg-alert.ts
- split-preview-presentation.ts

功能：

- 普通引用和 GitHub Alert 相互切换。
- Note、Tip、Important、Warning、Caution 类型菜单。
- Alert 自定义标题。
- 可视化和分屏预览分别装饰。
- Tab/Shift+Tab 调整普通引用层级。
- 修改后恢复原有光标位置。

────────────────────────────────────────────────────────────────────────────────

### 7. Details 折叠区域

主要文件：

- wysiwyg-details.ts
- details-selection.ts

功能：

- 识别由多个 Vditor 同级块组成的 <details> 区域。
- 支持嵌套 Details。
- 点击标题展开/收起。
- 单独编辑 <summary> 标题。
- 折叠时隐藏对应内容块。
- 右键可以删除完整 Details 区域。


### 9. 工具栏与键盘

主要文件：

- toolbar.ts
- toolbar-shortcuts.ts
- editor-tab-policy.ts

功能：

- 自定义工具栏和模式菜单。
- 保存工具栏点击前的编辑选区。
- 可配置快捷键并检测冲突。
- 阻止快捷键穿透到 VS Code。
- Ctrl/Cmd+M 控制 Tab 是否移动焦点。
- 列表、引用、表格、代码框使用各自的 Tab 行为。

─────────────────────────────────────────────────

### 10. 剪贴板、链接和媒体

┌────────────────────────┬──────────────────────────────────────────────────────────────────┐
│ 文件                   │ 功能                                                             │
├────────────────────────┼──────────────────────────────────────────────────────────────────┤
│ editor-clipboard.ts    │ Markdown/HTML 复制、代码纯文本复制、全块剪切、源码模式纯文本粘贴 │
├────────────────────────┼──────────────────────────────────────────────────────────────────┤
│ utils.ts               │ 链接点击策略、图片和链接弹窗目标处理                             │
├────────────────────────┼──────────────────────────────────────────────────────────────────┤
│ src/link-handler.ts    │ 安全协议校验、本地文件解析、目录打开                             │
├────────────────────────┼──────────────────────────────────────────────────────────────────┤
│ vditor-image-viewer.ts │ 替代被 CSP 阻止的图片查看器关闭事件                              │
├────────────────────────┼──────────────────────────────────────────────────────────────────┤
│ main.ts 中代码复制逻辑 │ 替代 Vditor 被 CSP 阻止的内联复制事件                            │
├────────────────────────┼──────────────────────────────────────────────────────────────────┤
│ src/webview.ts         │ 上传文件签名、MIME、大小和路径校验                               │
└────────────────────────┴──────────────────────────────────────────────────────────────────┘

────────────────────────────────────────────────────────────────────────────────

### 11. Split View 增强

┌───────────────────────────────┬────────────────────────────────────┐
│ 文件                          │ 功能                               │
├───────────────────────────────┼────────────────────────────────────┤
│ split-scroll-sync.ts          │ 源码与预览双向滚动同步             │
├───────────────────────────────┼────────────────────────────────────┤
│ split-preview-presentation.ts │ Front Matter 和 Alert 展示         │
├───────────────────────────────┼────────────────────────────────────┤
│ sv-code-indent.ts             │ 修复 Vditor 分屏代码块首行缩进丢失 │
├───────────────────────────────┼────────────────────────────────────┤
│ scroll-target.ts              │ 动态寻找当前实际滚动容器           │
└───────────────────────────────┴────────────────────────────────────┘

────────────────────────────────────────────────────────────────────────────────

二、明确存在的重复

### 1. HTML 透明预览类重复添加

以下两个模块都在添加：

```css
vmd-html-transparent-preview  
```

- wysiwyg-source-editor.ts
- wysiwyg-html-presentation.ts

可以统一由展示模块负责，源码编辑模块不必重复处理。

────────────────────────────────────────────────────────────────────────────────

### 2. 工具栏 Action ID 和快捷键解析重复

以下两侧各维护了一份完整 Action ID：

- src/toolbar-shortcuts.ts
- media-src/src/toolbar-shortcuts.ts

同时按键别名、保留快捷键和解析规则也部分重复。

建议建立共享 schema 或由构建脚本生成两侧定义，避免新增按钮时只改到一侧。

────────────────────────────────────────────────────────────────────────────────

### 3. Front Matter 展示构建重复

WYSIWYG 和 Split View 都分别实现了：

- 表格展示
- 代码展示
- data-vmd-source
- data-vmd-mode

建议提取统一的 buildFrontMatterPresentation(source, mode)。

────────────────────────────────────────────────────────────────────────────────

### 4. 原子块识别重复

代码块、公式块、HTML 块的：

- Selector
- 类型判断
- 源码节点查找
- 隐藏源码
- 删除和焦点恢复

分散在：

- wysiwyg-code-block.ts
- wysiwyg-source-editor.ts
- block-context-menu.ts
- editor-clipboard.ts

可以提取 wysiwyg-block-model.ts，统一提供 BlockKind、源码节点和预览节点解析。

────────────────────────────────────────────────────────────────────────────────

### 5. 菜单交互重复

Alert、标题、表格和块菜单都分别实现了：

- 创建按钮
- PointerDown 防丢失选区
- Escape 关闭
- 方向键移动
- 当前项状态
- Outside click 关闭

虽然已经有 floating-panel.ts，但它只负责定位和关闭。

可以继续提取通用的菜单键盘导航和菜单项构造；表格、块菜单的业务动作仍应分开。

────────────────────────────────────────────────────────────────────────────────

### 6. 两套弹层生命周期

目前有：

- floating-panel.ts：菜单类弹层，滚动时关闭。
- wysiwyg-popover.ts：编辑类弹层，滚动时重新定位。

两者不能直接合并，因为关闭策略不同，但可以共用底层的：

- Escape/Outside click 监听器
- Focus 恢复
- Resize/Scroll 订阅与释放

────────────────────────────────────────────────────────────────────────────────

三、比“合并”更需要拆分的文件

### wysiwyg-code-block.ts，约 1200 行

建议拆为：

1. ordinary-code-editor.ts：原位编辑。
2. atomic-block-navigation.ts：代码/公式/HTML 的选中、删除、前后光标。
3. code-block-presentation.ts：语言栏、编辑和复制按钮。
4. code-block-markdown.ts：源码范围和剪贴板 Markdown 提取。

### wysiwyg-popover.ts，约 1000 行

建议拆为：

1. source-edit-session.ts
2. popover-positioner.ts
3. link-image-popover.ts
4. popover-lifecycle.ts

### main.ts 和 src/webview.ts

两者都超过 1000 行，分别混合了初始化、同步、主题、上传、消息和滚动逻辑。适合按职责拆分，但不建议现在顺手重构。

────────────────────────────────────────────────────────────────────────────────

四、不建议删除的“小逻辑”

这些代码看起来小或特殊，但目前都有明确用途：

- preload.ts：兼容 Vditor 依赖的 global。
- vditor-image-viewer.ts：处理 CSP 阻止的内联关闭事件。
- 代码复制代理：处理 CSP 阻止的 Vditor 内联复制。
- sv-code-indent.ts：修复 Vditor 已知缩进问题。
- Front Matter 空行恢复。
- host-update-policy.ts：防止输入法和宿主更新互相覆盖。
- scroll-target.ts：不同模式实际滚动节点不固定。

结论

- 没有明显可以直接删除的整项功能。
- 最值得合并的是：快捷键 schema、Front Matter 展示、原子块模型、菜单基础交互。
- 最值得拆分的是：wysiwyg-code-block.ts、wysiwyg-popover.ts、main.ts、src/webview.ts。
- 不建议一次性重构；优先处理工具栏定义重复和 HTML class 重复，风险最低。
